[package]
name = "kubeli"
version = "0.3.86"
description = "Kubeli - Modern Kubernetes Management Desktop Application"
authors = ["Kubeli Team"]
edition = "2021"
license = "MIT"

[build-dependencies]
tauri-build = { version = "2.6", features = [] }

[dependencies]
# Tauri Core
tauri = { version = "2.11", features = ["macos-private-api", "tray-icon", "image-png"] }
tauri-plugin-shell = "2.3"
tauri-plugin-fs = { version = "2.4", features = ["watch"] }
tauri-plugin-dialog = "2.5"
tauri-plugin-store = "2.4"
tauri-plugin-updater = "2.10"
tauri-plugin-window-state = "2.4"
tauri-plugin-deep-link = "2.4"
tauri-plugin-single-instance = { version = "2.4", features = ["deep-link"] }
tauri-plugin-log = "2.8"
tauri-plugin-os = "2.3"
tauri-plugin-opener = "2.5"

# Kubernetes Client
kube = { version = "4.0", features = ["client", "config", "derive", "ws", "runtime"] }
k8s-openapi = { version = "0.28", features = ["v1_35"] }

# JSONPath for CRD custom columns
jsonpath-rust = "1.0"
dirs = "6.0"

# Async Runtime
# Enumerated features instead of "full": drops unused subsystems (signal,
# parking_lot, test-util, ...) from the build
tokio = { version = "1.52", features = ["rt-multi-thread", "macros", "sync", "time", "process", "io-util", "net", "fs"] }
tokio-util = { version = "0.7", features = ["io", "compat"] }
futures = "0.3"
futures-util = "0.3"

# HTTP & Networking
reqwest = { version = "0.13", default-features = false, features = ["json", "rustls"] }
hyper = "1.10"
hyper-util = "0.1"
tower = { version = "0.5", features = ["filter", "retry", "util"] }
tower-http = { version = "0.7", features = ["trace"] }

# WebSocket (for logs/exec streaming)
tokio-tungstenite = "0.30"
tungstenite = "0.29"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"

# OIDC Authentication
openidconnect = "4.0"
keyring = { version = "3", features = ["apple-native", "windows-native", "linux-native"] }
sha2 = "0.11"

# Cryptography & Security
rustls = { version = "0.23", features = ["ring"] }
secrecy = "0.10"

# Utilities
anyhow = "1.0"
thiserror = "2.0"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
base64 = "0.23"
chrono = { version = "0.4", features = ["serde"] }
rand = "0.10"
tauri-plugin-process = "2.3"
flate2 = "1.1"
url = "2.5"
uuid = { version = "1.23", features = ["v4"] }
regex = "1.12"
percent-encoding = "2.3"

# SQLite for AI session persistence
rusqlite = { version = "0.40", features = ["bundled"] }

# MCP (Model Context Protocol) Server
rmcp = { version = "2.1", features = ["server", "transport-io", "macros"] }
clap = { version = "4.5", features = ["derive"] }

# TOML for Codex config
toml_edit = "0.25"
http = "1"
hyper-timeout = "0.5.2"
x509-parser = { version = "0.18.1", default-features = false }

# macOS window management for tray popup
[target."cfg(target_os = \"macos\")".dependencies]
cocoa = "0.26"
objc = "0.2"
block = "0.1"

[dev-dependencies]
tempfile = "3.15"

[features]
default = ["custom-protocol"]
custom-protocol = ["tauri/custom-protocol"]

[profile.dev]
incremental = true
codegen-units = 256
split-debuginfo = "unpacked"

[profile.dev.package."*"]
opt-level = 1
debug = 0

[profile.release]
lto = true
opt-level = "s"
strip = true
codegen-units = 1
panic = "abort"
